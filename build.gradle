buildscript {
    dependencies {
        // ✅ Firebase 설정용 Google 서비스 플러그인 등록
        classpath 'com.google.gms:google-services:4.4.1'
    }
}

plugins {
    alias(libs.plugins.android.application) apply false
}
